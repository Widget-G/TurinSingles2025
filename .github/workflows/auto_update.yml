name: Auto Update ATP Finals 2025 ICS

on:
  schedule:
    - cron: "0 */6 * * *"  # ⏰ co 6 godzin
  workflow_dispatch:       # ➕ możliwość uruchomienia ręcznie

jobs:
  update-ics:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repo
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install icalendar requests beautifulsoup4 pytz

      - name: Generate updated ICS
        run: |
          python generate_ics.py

      - name: Commit and push changes
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git add output/ATP_Finals_2025_Singles_Turin.ics || echo "⚠️ Brak zmian do dodania"
          git commit -m "Auto-update ATP Finals 2025 schedule" || echo "⚠️ Brak zmian do commitowania"
          git push || echo "✅ Repozytorium jest aktualne"

      - name: Send Push Notification (ntfy)
        if: success()
        run: |
          curl -H "Title: 🎾 ATP Finals 2025 Update" \
               -H "Priority: 4" \
               -H "Tags: tennis,calendar,rocket" \
               -d "Nowy plan meczów został opublikowany i Twój kalendarz .ics został zaktualizowany! 📅" \
               https://ntfy.sh/kgforce_atp2025
